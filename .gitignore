# ── 项目 ──────────────────────────────────────────
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/

# ── 环境与密钥（绝不入库）──────────────────────────
.env
*.env
.venv/
venv/

# ── 生成物（可由脚本重建）──────────────────────────
*.index
*.faiss
data/index/
data/bench/*.generated.*

# ── 内部文档：含求职策略与口径约定，不进公开仓库 ──────
PLAN.md

# ── 不公开：本仓库为个人作品集，不含任何文献原文 ──────
# 只保存 PDF 路径与 Zotero item key，绝不复制论文文件
data/library/
*.pdf

# ── 编辑器 / 系统 ─────────────────────────────────
.idea/
.vscode/
.DS_Store
Thumbs.db
