# RUN_LOG — 迭代与失败记录

> 记录设计取舍、踩过的坑、bad case。面试时「你怎么定位失败」的答案来源。

---

## 2026-09-19 · Day 0：方向收敛与可行性摸底

**结论**：三份独立调研从三个方向指向**同一个空白** —— 论文内部数值自洽审查。

| 调研覆盖面 | 判定原话 |
| --- | --- |
| 通用评测/工具生态 | 「论文内部数值自洽性检查的公开工具或产品：**未找到**」；PaperQA2（9,219★）也不做数值核算 |
| 遥感领域基准 | 遥感/地学**文献问答**公开基准：**未找到**；中文遥感文献 QA 基准：**未找到** |
| Zotero 生态（11 个项目，含 5,083★ zotero-mcp） | 「以上项目**都没有**『论文数值自洽性审查』这类可核验断言能力」 |

### 解析器选型：Docling 胜出（实测）

在同一篇论文上实测 Docling vs MinerU：

| | Docling | MinerU 4.0.3 |
| --- | --- | --- |
| 许可 | **MIT** | Apache-2.0 + 附加条款 |
| 本地跑 | ✅ 开箱可用 | ⚠️ 仅 `flash` 档可用；高质量档需另起 parse-server + 模型权重 |
| **表格** | ✅ 9 个表格 → 正确 DataFrame | ❌ flash 档无表格 |
| caption 独立类 | ✅ `caption=20` | 需高质量档 |
| 页码 provenance | ✅ 311/311、315/315 全覆盖 | ✅ `<!-- page N of M -->` |

**决定性差异**：数值自洽审查必须拿到**表格结构**并**区分摘要/表格/图注**。Docling 本地开箱就给，MinerU 要拿到得先搭服务。MinerU 的 `flash` 快路径价值又被 Zotero 的全文缓存吃掉了 → **不引入第三个组件**。

### ❌ 失败 1：conda-forge 装不上 Docling

`deepsearch-glm` 只有 PyPI 有 → **Docling 必须走 PyPI**。

### ❌ 失败 2：Docling 与主环境依赖硬冲突

`typer==0.27.2`、`huggingface-hub==1.31.0` 被 conda solve **pin 住**，docling 要求不同版本 → 无解。
试过 `index-strategy = "unsafe-best-match"` 无效。

**解法**：把解析器**隔离成独立环境**（`no-default-feature = true`，不继承主环境依赖）。主环境保持干净，解析器以子进程 worker 形式调用。这比强行混装更合理。

### ⚠️ 修正 1：成本模型从 40 小时降到 5 小时

最初被一篇 1982 年扫描件（**668 秒**）吓到，推算全库要 40 小时。抽样 40 篇实测：**95% 有文本层**（26s/篇），仅 5% 需 OCR → **全库 474 篇约 5 小时**。

**教训**：单点样本不能外推。**抽样测分布**才算成本。

### ⚠️ 修正 2：PyMuPDF 是 AGPL-3.0

前几轮方案默认用它做 PDF 解析 —— **AGPL 会污染开源许可**。已移出依赖，README 显式声明不使用。

### ⚠️ 发现 3：真实文献库里必然有畸形 PDF

`Shang 等 - 2023` 那篇 PyMuPDF 报 **页数 0**，Docling 报 `not valid`。查 Zotero 侧：**它也没有 `.zotero-ft-cache`** —— 说明 **Zotero 同样抽不出，没有兜底**。
→ 流水线必须「跳过 + 写日志」，不能假设所有 PDF 可解析。

---

## 2026-09-19 · Day 1：Zotero 适配层

**产出**：`src/rsrc/config.py`、`src/rsrc/zotero/library.py`、`tests/test_zotero.py`。

**验收**：`4/4 通过`。完整链路打通 —— `itemKey → children → attachment → file/view/url → file:// 路径 → 文件存在`，5/5 验证。

### 库的实测形态（作者本机）

```text
Zotero 10.0.2 (64-bit)   API v3 / Schema v44 / Server-ID KUxHSVVBgELj
数据目录      F:\ZoteroPaper（prefs.js 自定义，不是默认 ~/Zotero）
条目（2000 条样本）
  annotation      799
  attachment      645
  journalArticle  480      ← 研究文献主体
  conferencePaper  16
  thesis            6
  dataset           6
storage/  751 目录 / 474 PDF（3.3 GB）
fulltext.sqlite  25 MB，726 篇已索引（其中 CJK 249 篇）
```

### 🐛 Bug 1：`linkMode` 在 API 里是**字符串**，不是整数

**现象**：自检报「5 篇期刊论文里没找到任何可用 PDF」，但手工 curl 明明能拿到。

**定位**：`config.py` 按 Zotero **源码常量**写成了整数 `(0, 1, 2)`，而 **HTTP API 返回的是 `"imported_url"` 这样的字符串**。于是 `linkMode not in (0,1,2)` 恒为真，**所有附件被静默过滤**。

**修法**：字符串与整数两套都认（SQLite 侧确实是整数），内部统一归一成名字。

> **教训**：官方**源码常量**与**API 序列化形式**是两回事。文档给的枚举值不一定是线上传的形态 —— **以实测响应为准**。
> 另一个教训：`attachment_path()` 里把异常吞成 `None` 是必要的健壮性，但也**掩盖了真错误** —— 幸好自检在端到端层面把它逼出来了。

### ⚠️ 发现 4：`/fulltext` 返回 JSON，不是纯文本

返回体是 `{"content": "..."}`，**必须解包**。实测单篇 41,601 字符。
这是「档 0」快速通道 —— 省去自己解析 PDF，但**无页码无版式**。

### ⚠️ 发现 5：附件路径是 URL 编码的

`file:///F:/ZoteroPaper/storage/RKA4MEL9/Zhang%20%E7%AD%89%20-%202015%20-...`
→ 必须 `unquote` 并处理 `file:///F:/` 前导斜杠，否则 Path 不可用。

---

---

## 2026-09-19 · Day 1 续：Embedding 选型实测

### 调研结论修正了优化目标

按 474 篇 × ~8,000 token ≈ **3.8M token** 估算：

| 渠道 | 全量成本 | 结论 |
| --- | --- | --- |
| 百炼 `text-embedding-v4` | 免费额度 100 万 token 后约 **¥1.4** | 中文最强（同族 8B C-MTEB 73.84） |
| SiliconFlow `BAAI/bge-m3` | 标价 ¥0/M | **实测零余额被拒，见下** |
| OpenRouter `:free` | $0 | ⚠️ 无充值仅 **50 请求/天**（要跑 12 天）+ `:free` 变体「**可能被留存用于训练**」 |
| 本地 fastembed | **¥0** | 见下 |

**核心判断**：这个规模下**免费额度根本不是瓶颈**（全量最多约 2 元）。该优化的是**中英混合检索质量** —— 库里中文占 **249/474 ≈ 52%**，接近一半。

### ❌ 失败 4：SiliconFlow 零余额账户全线 402

调研查到 `BAAI/bge-m3` 在 SiliconFlow 标价 **¥0/M token**，据此推断可以零成本使用。

**实测**：`BAAI/bge-m3`、`Pro/BAAI/bge-m3`、`Qwen/Qwen3-Embedding-0.6B`、`BAAI/bge-large-zh-v1.5` **全部返回 `402` "Sorry, your account balance is insufficient"**。
`/v1/user/info` 也返回 `410 deprecated`，查不到余额。

**教训**：「模型单价为 0」≠「零余额可用」。平台可能仍要求账户有正余额。**价格页数字不能替代一次真实的 API 调用验证。**

### ❌ 失败 5：`.env` 里写死 `EMBED_MODEL` 会跨 provider 泄漏

`.env` 写了 `EMBED_MODEL=BAAI/bge-m3`，切到 `EMBED_PROVIDER=local` 后这个值被沿用 → fastembed 报 `Model BAAI/bge-m3 is not supported`。

**根因**：`EMBED_MODEL` 是全局的，但模型名是 **provider 私有**的。
**修法**：`.env` 不再写 `EMBED_MODEL`/`EMBED_DIM`（由 provider 预设驱动），只在需要覆盖时才填。

### ❌ 失败 6：fastembed 不支持 bge-m3

`TextEmbedding.list_supported_models()` 实测 30 个模型，**不含 `BAAI/bge-m3`**。
多语/中文可选：`MiniLM-L12-v2`(384/0.22GB)、`mpnet-base-v2`(768/1.0GB)、`multilingual-e5-large`(1024/2.24GB)、`jina-v2-base-zh`(768/0.64GB)、`bge-small-zh-v1.5`(512/0.09GB)。
→ 默认改用 **`paraphrase-multilingual-mpnet-base-v2`（768 维）**，并在代码里加了「不支持就列出可选模型」的报错。

### ✅ 本地兜底实测：可行

| 指标 | 实测 |
| --- | --- |
| 模型 | `paraphrase-multilingual-mpnet-base-v2`，768 维 |
| 首次调用（含 ONNX 初始化） | 3.0 s |
| **预热后吞吐** | **293 ms/chunk（3.4 条/秒）** |
| **全库 13,000 chunk 外推** | **约 64 分钟** |

> ⚠️ 注意第一次粗测得 22,307 ms/条 —— 那是**首次调用把模型初始化也算进去了**（5 条样本）。
> **教训**：性能测量必须先预热、且样本量足够，否则会得出「慢 76 倍」的错误结论。

语义区分正常（实测余弦）：「地表发射率地形校正」↔「terrain correction of LSE」**0.7766** vs 无关句 **0.2749**。

### 结论

**默认走本地兜底**：¥0、离线、不需要 key，符合「本地可跑通」验收标准，约 1 小时建完索引。
有 API key 的用户可切百炼拿到更好的中文检索质量（约 ¥1.4），**但换模型必须全量重建索引**（`EmbeddingSpec.fingerprint()` 绑定）。

---

## 2026-09-19 · Embedding 定选：SiliconFlow `BAAI/bge-m3`

账户充值后重测，**402 消失**，但仍撞到一个 400 —— 详见下。

### ❌ 失败 7：给固定维度模型传 `dimensions` 会 400

`BAAI/bge-m3` 是**固定 1024 维**（不支持 MRL 降维），但我的代码无条件发送
`dimensions` → SiliconFlow 返回 `400 code 20015 The parameter is invalid`。

**修法**：**只在用户显式设置 `EMBED_DIM` 时才向 API 请求降维**；预设里的维度只用于
`EmbeddingSpec`（指纹与索引元数据），不发出去。
→ 原则：**「声明维度」与「请求降维」是两件事，不能混。**

### ⚠️ 发现 8：限流会触发 SDK 静默重试，表现为单个批次异常慢

批量吞吐实测（64 条一批，文本约 400 token）：

| 批次 | 耗时 | 每 chunk |
| --- | --- | --- |
| 16 条 | 0.24 s | 15 ms |
| 32 条 | 0.33 s | 10 ms |
| **64 条（第一次）** | **61.26 s** | **957 ms** |
| 64 条（第二次） | 0.68 s | 11 ms |

第一次 64 条慢了 **约 90 倍** —— 不是模型慢，是**触发了限流后 `openai` SDK 自动退避重试**。

**结论**：批量入库**必须自己控速**（客户端令牌桶 + 按 `Retry-After` 退避），
不能依赖 SDK 的隐式重试 —— 否则进度不可预测、日志看不出原因。

### 定选对比

| | 本地 fastembed `mpnet-base-v2` | **SiliconFlow `BAAI/bge-m3`** |
| --- | --- | --- |
| 全库 13,000 chunk | **约 64 分钟** | **约 11 分钟**（受 TPM 500k 约束） |
| 维度 / 上下文 | 768 / 512 | **1024 / 8192** |
| 中英混合 | 可用 | **原生多语（100+）** |
| 成本 | ¥0 | 约 ¥1.4 |
| 需网络 | 否 | 是 |

**结论**：默认用 SiliconFlow `bge-m3`；本地 fastembed 保留为**离线兜底**
（无 key 时自动启用，符合「本地可跑通」）。

语义区分验证（bge-m3）：同义 `0.7419` vs 无关 `0.3248`。

---

## 2026-09-19 · Day 2：解析层 + 数值抽取可行性验证

**产出**：`scripts/parse_pdf.py`（Docling worker，parser 环境）、`src/rsrc/audit/extract.py`。

### 解析质量：在作者自己 4 篇论文上实测

| 论文 | 页 | 块 | **带页码** | 表格 | caption | 耗时 |
| --- | --- | --- | --- | --- | --- | --- |
| 论文1 JSTARS | 16 | 242 | **242 (100%)** | 2 | 21 | 155 s |
| 论文2 GRSL | 5 | 96 | **96 (100%)** | **0** | 4 | 46 s |
| 论文3 IJRS | 26 | 204 | **204 (100%)** | 3 | 16 | 140 s |
| 论文4 IJRS | 25 | 221 | **221 (100%)** | 1 | 13 | 107 s |

**页码 provenance 四篇全部 100%** —— 引用溯源与数值定位的前提成立。
表格抽成了正确的行列表（实测 Table 1 的数据集清单列对齐正确）。
`caption` 与 `text` / `section_header` 分开 —— C1/C2 要比较的「摘要 vs 表格 vs 图注」可区分。

### ⚠️ 修正 3：解析成本比先前估计高 4 倍

实测 **~113 s/篇**（9–14 MB 的正式出版版，图多）→ 474 篇外推约 **15 小时**，
而 Day 0 抽样得出的 26 s/篇外推是 5 小时。

**教训**：又一次「抽样不代表整体」。Day 0 的样本是原生小 PDF，这次是真实出版版。
**成本估算必须用目标总体的随机样本，不能用便利样本。**

### ⚠️ 发现 9：数值抽取是真正的难点（这是本项目的核心工作量）

第一版按「`指标 = 数值`」写正则 → **在 4 篇真实论文上命中 0 处**。

实测到**至少 8 种真实书写形态**：

```text
R² ≈0.73                          约等号
R ² of 0.981                      上标前有空格 + 连接词 of
R 2 reduced from 0.055 to 0.0036  上标被拆成「空格+2」；区间式
an RMSE of 0.0125                 「指标 of 值」
reduced RMSE (23.88 mm/month)     括号 + 单位
bias < 0.001, RMSE < 0.05         不等式
reached ~0.02                     波浪号近似
A VHRR                            单词内被插入空格（反向噪声）
```

**科学论文的数值是散文式表达，不是键值对。** 加入归一化层（`R ²`/`R 2`/`R2` → `R²`、
破折号 → 减号、全角 → 半角、数字被空格拆开）后，命中从 **0 → 21 条**。

> 这个「0 → 21」正是本项目价值的实证：**朴素做法拿不到数据，而这恰恰是通用 RAG 不会去碰的地方。**

### ⚠️ 发现 10：指标词表不能写死

论文2（GRSL）抽到 **0 条**，查原文发现它**根本不用 R²/RMSE/Bias 这套词** ——
报告的是 `0.004 decline of discrepancies`、`0.01 decline of discrepancies` 这类**领域特有表述**。

**含义**：C1–C6 必须**指标无关**，或支持用户自定义指标词表；否则会**静默漏掉整篇论文**。
（论文2 的表格数也是 0 —— GRSL 短文可能把结果放在图里而非表中。）

### 🐛 Bug 2：`context` 字段为空 —— 相对/绝对偏移混用

`NumericFact.context` 用于人工核验，但初次实现里它是空的。
**根因**：`num_end` 是相对 `rest` 的偏移，却直接拿去切 `text`。
**修法**：换算回绝对坐标 `match.end() + num_end`。

> 这类 bug 不会抛异常，只会让字段静默为空。**自检必须断言字段非空，不能只看「没报错」。**

### 当前抽取结果（归一化后）

| 论文 | 条数 | 明细 |
| --- | --- | --- |
| 论文1 JSTARS | 5 | RMSE 3（p1 23.88 mm/month、p2 0.0125、p2 0.0613）；R² 2（p2 0.981、0.973） |
| 论文2 GRSL | 0 | 指标词表不匹配（见发现 10） |
| 论文3 IJRS | ~10 | R² 3（p2 ≈0.73、p3 0.783、p19 ≈0.73）；RMSE 2（<0.05、0.02）；Bias 4 |
| 论文4 IJRS | 6 | R² 5（p2 ≈0.75、p17、p18）；Bias 1（p3 0.02） |

**下一轮要补的连接方式**：`reduced from X to Y`（论文4 摘要的核心发现就用了这个，
当前未抽到）、`reaches ~X`、`higher/lower than X`。

---

## 2026-09-19 · Day 2 续：抽取器重做（21 → 66 条）+ 作者核验

### ✅ 作者人工核验：抽取结果无误报

作者（论文一作）逐条核验了抽取结果，确认：

- 论文1 `R² = 0.981` / `0.973`、`RMSE = 23.88 mm/month` —— **属实**
- 论文4 p2 `R² ≈ 0.75`（TRI 相关）—— **属实**
- 论文3 的 `0.73` 与 `0.783` —— **属实且合理**：两处**对象不同**，不是同一数字被解析两次

→ **误报率为 0**（在已核验范围内）。这是 C2 能否成立的关键前提。

### ⚠️ 修正 4：我指错了行 —— p17 是对的，p16 才是坏的

我怀疑论文4 p17 的 `R² ≈ 0.0` 是 `0.0036` 被截断。查原文：

```text
p17  (e.g. R 2 ≈0 for Slope >20°)          ← 原文就是「约为 0」，抽取正确
p16  ( R 2 0 : 055 for Slope >0°)        ← 这里才是真 bug
        ↑ U+FFFD（'=' 解码失败）  ↑ 小数点被渲染成冒号
```

**p16 的真实缺陷有两处**：① `=` 变成替换字符 **U+FFFD**；② **小数点变成了冒号**（`0 : 055`）。
而 `R² = 0.055` 这个值**此前根本没被抽出来**（`R 2 0 : 055` 不匹配任何模式）。

> **教训**：作者口头核验与代码证据冲突时，**回去看原始字节**（`repr()` + 码点），
> 不要顺着对方的结论改代码。这次若照做，就会把正确的抽取"修"坏，
> 而真正坏的那条继续漏掉。

### 重构：从「枚举连接词」改为「扫描窗口」

原实现枚举连接词（`= : of is was reached ( …`），**必然漏**。
改为：**指标名之后跳过一段不含数字、不跨句的字符（≤44），再读数值**；关系（`< > ≈ =`）在该窗口内识别。

| | 抽取条数（4 篇真实论文） |
| --- | --- |
| 枚举连接词版 + 归一化 | 21 |
| **扫描窗口版 + 归一化** | **66** |

新增归一化规则：
- 小数点被渲染成冒号：`0 : 055` → `0.055`
- 移除 U+FFFD（实测它替掉了 `=`），并折叠由此产生的多余空白

### 新增自检 `tests/test_extract.py`（9 项，全通过）

用例**全部来自真实论文的实测形态**，不是构造的：
`R²`/`R ²`/`R 2`/`R2` 归一、`0 : 055` 修复、区间式 `from X to Y`、
位置信息随事实返回、`context` 非空、`picture` 块跳过、
以及在真实解析产物上的回归（断言总数 ≥20）。

> 之所以要写这套自检：**本轮两个 bug（`context` 为空、小数点变冒号）都不抛异常**，
> 只让字段静默为空或静默漏抽。**这类 bug 只能靠断言字段内容来抓。**

---

## 2026-09-19 · Day 3：C1–C6 检查项（误报 8 → 0）

**产出**：`src/rsrc/audit/checks.py`、`tests/test_checks.py`（12/12 通过）。

**验收**：真实 4 篇论文上 **high 0 / medium 0 / info 1**（那条 info 还明确标注"不同数据集属正常，需人工确认"）。

### 核心纪律：**误报是这工具的死穴**

首版在 4 篇真实论文上产生 **8 条 findings，全是误报**。
逐轮定位、逐个修，**每一条的根因都是真 bug**：

| 轮 | 误报现象 | 根因 | 修法 |
| --- | --- | --- | --- |
| 1 | `R² =0.055` 与 `R² to0.0036` 被判"同一对象冲突" | `reduced from 0.055 to 0.0036` 是**一个区间声明**，不是两个独立值 | C2 排除 `relation="to"` |
| 2 | `Bias =5000`（其实是尺度） | 盲扫窗口跨过了分句：`LST bias **using DEMs matched to the** 5000 m` | 改为**连接词白名单 token 扫描** |
| 3 | 对象抽成 `strong` | context 只取左侧 20 字符，把 `(TRI)` **截断**，括号正则匹配不到 | 左侧窗口 20 → 70 |
| 4 | `R² ≈0.75` 与 `R² ≈0.13` 被判冲突 | 对象抽成 `r² ≈`（括号里装的就是「指标+值」本身）；而用「指标 token 是否为子串」判定又因 `R²`→`r` 单字母而**误杀合法对象 `TRI`** | 改为「剔掉数字与指标名后看还剩不剩实词」 |
| 5 | `for TRI R² = 0.21` 的对象被抽成上一句的 `strong` | `ctx.partition(metric)` 取的是**第一个**同名指标 | 改 `rpartition`（最后一次出现） |
| 6 | 校准前的 Bias 与校准后的 RMSE 互比 | 句末正则用 `(?<!\d)` 排除小数点，**把「数字后紧跟句号」也误排除** → `0.02. After` 不算断句 | 改为「标点后跟空白+大写 / CJK」才算断句 |
| 7 | `evergreen needleleaf forests` 与 `evergreen broadleaf forests` 撞车 | 右侧对象只取**第一个**实词 | 取前 3 个实词 |
| 8 | 不同数据集的站点数被判冲突 | 同一段/句里多批站点数（AmeriFlux / European / 高原）是**合法**的 | 对象改为取**右侧数据来源**，并在第一个句读处截断 |

### 检查项的最终分层

| 编号 | 判定依据 | 可靠性 |
| --- | --- | --- |
| **C3** | 纯数学关系（`R²∈[0,1]`、`RMSE≥MAE`、`|Bias|≤RMSE`），**限定在同一对象的同一句** | 最高 |
| **C2** | 同一指标 + 同一对象，跨位置取值不一致 | 高（对象抽不准就放弃比较） |
| **C1** | 同一**来源**的样本数不一致 | 高 |
| **C5** | 同一指标用了多种单位 | 中（报 info） |

**三条防误报护栏**（写进代码注释）：
1. **对象抽不准 → 放弃比较**（宁漏报不误报）
2. **区间端点 / 不等式 → 不参与等值比较**
3. **数学关系只在同一对象的同一句内成立**（跨句比较必出误报）

### 召回验证（`tests/test_checks.py`）

注入式用例证明「能抓到」：`RMSE < MAE`、`|Bias| > RMSE`、`R² = 1.24`、
同一对象跨位置冲突、同一来源样本数不一致 —— **全部被抓到**。

同时用**回归用例**锁住上面 8 类误报，防止后续改动把它们放回来。

> **这一轮的价值**：验证了「论文数值自洽审查」这件事**技术上成立且可做到零误报** ——
> 但它**绝不是「写个正则比对一下」**。首版 8 条误报全部来自「想当然」，
> 每一条都要回到原始字节才能定位。这就是三份调研共同确认的空白为什么至今没人填。

---

## 2026-09-19 · Day 4：LangGraph 编排 + 端到端可演示

**产出**：`src/rsrc/graph.py`、`src/rsrc/report.py`、`src/rsrc/audit/inject.py`、
`scripts/run_audit.py`、`tests/test_graph.py`。

**全部自检 33/33 通过**：extract 9/9 · checks 12/12 · graph 4/4 · zotero 4/4 · llm 4/4。

### 状态图

```text
load ─▶ extract ─▶ check ─┬─(有矛盾 且 LLM 可用)─▶ summarize ─┐
                          └─(否则)────────────────────────────┴─▶ render
```

**`summarize` 做成条件边**的两个理由：
1. 没有矛盾时让 LLM 写"没发现问题"纯属浪费 token
2. **没配 LLM 时整条链路降级为纯确定性报告，而不是失败** ——
   核心检查项本来就不依赖 LLM，这正是「降幻觉」的设计前提

LLM 摘要的 prompt 里写了硬约束：**只允许提及已核验的数值与位置，不得引入新数字**。
且即便 LLM 出错，报告里的 finding 列表与事实表仍可核验。

### 端到端演示（论文4，作者一作）

| 场景 | 结果 |
| --- | --- |
| 真实论文 | 13 条事实 / **0 finding** |
| 注入 4 处已知缺陷 | **4/4 全部被对应检查项抓到** |

注入是**在真实文本上原地改**（不是拼假论文），每次记录
`{类型, 位置, 原值, 新值, 期望检查项}` 作为 ground truth —— 这就是 Day 5 评测的底子。

### 🐛 Bug 3 / 4：注入器自身的两个坑（都伪装成"检查器漏报"）

| 现象 | 真因 |
| --- | --- |
| 注入的 C1 抓不到 | 注入时**重新拼了**来源名（`from AmeriFlux`），而原文是 `from the AmeriFlux Dataset`；两边对象串不同，检查器**正确地**判为不同来源 |
| 同上 | 来源短语只吃到首个大写词（`from the AmeriFlux`），注入点插在了 `Dataset` **之前**，导致被逗号截断 |

> **教训**：注入器出问题时，症状看起来像"检查器漏报"。**做 ground truth 的代码本身
> 也需要被怀疑** —— 否则会去"修"一个本来正确的检查器（与 Day 2 那次「指错了行」同源）。

### 顺手修：测试里的魔法数字

`test_llm` 断言本地兜底为 384 维，但预设已改为 768 → 假失败。
改为**断言与预设一致**（`PROVIDER_PRESETS["local"]["embed_dim"]`），预设调整不再造成假失败。

### 报告设计原则

- **每一条结论都可溯源**：每个数值都带 `p<页码>/<block类型>` + 原文片段
- **报告自己也不能产生新的不可核验断言**：末尾显式声明
  「检查项为确定性规则，**不含 LLM 的自由判断**；LLM 仅用于生成摘要段落」
- `reports/` 为生成物且含论文片段 → 已 gitignore，不入库
